import CreateConfession from '../atoms/CreateConfession';

const WriteConfession = () => (
  <div className='mt-32 mb-8 w-full'>
    <CreateConfession />
    {/* here we also perform onclick action to show modal */}
  </div>
);

export default WriteConfession;